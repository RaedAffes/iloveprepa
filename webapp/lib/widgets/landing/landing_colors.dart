import 'package:flutter/material.dart';

/// Landing-page colour tokens. Kept separate from the app's workspace
/// palette so the marketing page keeps its own look.
class AppColors {
  static const darkNavy = Color(0xFF16255E); // deepest background
  static const midBlue = Color(0xFF1B3FA0); // background base
  static const brightBlue = Color(0xFF2151C7); // secondary wave
  static const accentBlue = Color(0xFF2F6FED); // monitor / accents
  static const orange = Color(0xFFF5A623); // logo + button + pill
  static const lightBg = Color(0xFFF4F6FB); // page background
  static const white = Colors.white;
  static const mutedWhite = Color(0xCCFFFFFF);
}
